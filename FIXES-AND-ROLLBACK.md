# 本次修复与回退说明

基线：`1d07da2`（保留队友最新的网络提示与交易错误处理）。修复分支：`fix/verified-bugs`。

本次修复 **15 项问题**，按严重程度整理为 **3 个修复提交**，依次推送到 GitHub 的修复分支。代码、测试和说明包含在这三个提交中。合并到 main 由维护者通过 Pull Request 完成；本次没有部署线上网站或发送链上交易。

## 修复清单

| # | 修复的问题 | 验证 |
|---|---|---|
| 1 | AI 返回空对象、缺字段或非法值时，被错误当成有效低风险结果 | 格式与端到端模拟响应测试 |
| 2 | 总结忽略确定性警告；只有一个有效模型也被当成双模型通过 | 汇总规则、单模型及同名模型测试 |
| 3 | 跨批次“没有发现异常”被当成异常，AI 离线时误报 | 无异常、离线与不足两个批次测试 |
| 4 | 供应链角色都出现但顺序颠倒时漏报 | 正常、缺步骤和逆序测试 |
| 5 | 有效期没有进入确定性验证和模型证据 | 过期规则测试 |
| 6 | 跨批次短时间集中活动被旧记录掩盖；重复批次编号被误合并 | 时间窗口与相同编号不同对象测试 |
| 7 | 错误对象类型被当作药品；旧结构缺失有效期被默认为 1970 年 | 类型、原始包地址和旧结构测试 |
| 8 | RPC 返回成功被当成交易执行成功，失败交易进入成功回调 | 失败、缺少 effects、成功三种结果测试 |
| 9 | 总质押统计漏掉登记后的追加质押 | 实际统计函数汇总事件测试 |
| 10 | 每次打印从 1 开始，重复生成同一组标签编号 | 浏览器重复生成、切批次清理标签测试 |
| 11 | 扫码序号在查询时丢失，重复查询同一 ID 不刷新 | 浏览器查询保留序号测试；扫码回调代码检查 |
| 12 | 旧 AI 请求晚到后显示在别的批次／状态上 | 浏览器延迟返回、切批次测试 |
| 13 | 事件读取达到上限或缺少游标时，仍把部分数据当成完整结果 | 上限、缺游标、正常结束测试 |
| 14 | 查询失败或对象不兼容时，页面误称销售码已核销 | 浏览器 RPC 故障与不兼容对象测试 |
| 15 | AI 请求没有等待期限，单个挂起请求拖住整个结果 | 前端／服务端取消信号测试 |

发布批次：

- 第一批（6 项，#1、#2、#3、#5、#7、#8）：交易失败误报成功、验证误判和对象兼容性。
- 第二批（5 项，#4、#10、#11、#12、#14）：供应链顺序、标签与查询状态。
- 第三批（4 项，#6、#9、#13、#15）：跨批次窗口、统计完整性与请求超时。

细分修复历史仅保留在本地备份分支，不推送。每批均包含对应的回归测试。

## 做过的验证

- 合并前逐个验证测试基础与 15 项修复对应的独立源码快照；每个快照都运行当时已有的测试、前端 TypeScript 检查、`api/gonka.ts` 与 `vite.config.ts` 类型检查、Vite 生产构建。合并提交不会改变代码内容。
- 最终 **18 项自动化逻辑测试全部通过**。
- 隔离 Edge 浏览器验证：查询序号保留、重复打印编号不复用、切换批次清理旧标签、延迟 AI 结果不串批次、不兼容对象被拒绝、RPC 失败不冒充已核销，未捕获到页面未处理异常。
- 浏览器回归使用受控 RPC／AI 响应，不是本次新增的真实链上或钱包验收。
- 核对过最终代码与拆分前已审查的源码一致，没有在拆提交时遗漏修复。`contract/` 与基线没有内容差异。

这些检查降低回归风险，不能证明不存在任何新问题。真实部署、真实钱包和 Move 合约测试仍需在版本配置一致的测试环境完成。

## 如何复跑

在 `frontend` 目录执行：

```sh
npm ci
npm test
npm run build
npx tsc --noEmit --target ES2022 --module ESNext --moduleResolution Bundler --skipLibCheck api/gonka.ts vite.config.ts
npm run test:ui
```

浏览器测试默认使用本机 Edge，自动在 `127.0.0.1:5189` 启动并关闭测试服务。没有 Edge 时，可以先运行 `npx playwright install chromium`，再将 `TEST_BROWSER_CHANNEL` 环境变量设为 `chromium`。测试运行不连接钱包、不发送链上交易、不调用付费模型。

## 兼容性与行为变化

- 类型校验使用 `VITE_TYPE_PACKAGE_ID`，未设置时默认等于 `VITE_PACKAGE_ID`。普通新部署只需原来的设置；兼容升级后，前者填定义对象／事件类型的原始包，后者填当前交易调用包。查询事件的地方也统一使用原始类型地址。
- 老结构没有有效期时，显示“不兼容批次”，不会猜成已过期；这不是给旧数据自动补上有效期。跨批次查询会排除无法解析的对象。
- 标签改用 UUID 并允许文字换行；已打印的旧数字标签仍可被解析。新编号降低重复生成碰撞风险，**不等于链上单品唯一性**，也不能阻止复制纸质二维码。
- 有确定性警告时，AI 总结不再显示通过；单模型或同名双模型结果标记为需要复核。
- 服务端请求最长等待 120 秒，浏览器 125 秒。实际托管平台可能更早超时；超时后保留规则降级结果。
- 查询事件超过当前支持范围时不再静默展示部分“全量”统计，而是展示错误。后续若需要更大规模，应实现真正的分页／索引功能。

## 没有在本批修改的事项

- 合约中故意开放的角色权限与处罚金机制。它们有此前报告的风险，但属于权限／经济规则设计，不能因为本次前端修复就认为已经安全。
- 完整的链上单品登记、库存数量与销售绑定。
- 需要产品定义的温度范围、医疗真实性与实体药品交付证明。
- Vercel 线上合约地址、密钥和部署环境；本次没有修改线上设置。
- AI 代理的认证与持久化限流策略；本次只修复等待无期限的问题。

## Git 能回退多少个版本

Git 没有“最多回退 N 个提交”的固定限制。只要提交仍保存在仓库中，就可以基于它查看、创建分支或撤销相关改动。丢失引用、清理了对象或只下载浅历史时，可用历史会受影响，这与固定版本数量限制不同。

多人协作优先使用 `git revert`：它创建一个新提交撤销指定修改，不抹掉原来的历史。官方说明：https://git-scm.com/docs/git-revert

日后若要撤销某一批，可先通过 git log 找到该批修复提交 ID，再执行：

```sh
git revert <该批修复提交ID>
```

这会创建一个新的撤销提交。执行前保存工作区修改，撤销后重新运行测试和构建。不要把 reset --hard 加强制推送作为共享 main 的日常回退方式。

三个提交便于按批回退；若要全部撤销，通常按第三批、第二批、第一批的逆序撤销。若只想撤销某批中的一项，需要按文件或差异制作针对性的修改，再验证。AI 汇总、规则和查询页面之间存在关联，不能假设所有代码行都能互不影响地删除。

回退 Git 代码**不会自动回退已经发生的链上交易、已部署的合约状态或 Vercel 环境变量**。网站代码、部署配置和链上状态需要分别处理。
